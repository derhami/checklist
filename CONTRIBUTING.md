# 🤝 راهنمای مشارکت (Contributing)

از مشارکت شما در بهبود این پروژه سپاسگزاریم! لطفاً پیش از ارسال تغییرات، موارد زیر را مطالعه کنید.

---

## 🚀 شروع کار

1. مخزن را **Fork** کنید.
2. یک شاخه‌ی جدید با نام توصیفی بسازید:

   ```bash
   git checkout -b feature/my-feature
   ```

3. تغییرات را اعمال و commit کنید.

## 💻 توسعه‌ی محلی

```bash
npm install
npm run dev
```

- لینت/تایپ‌چک را همیشه اجرا کنید:

  ```bash
  npm run lint
  ```

- ساختار داده‌ی چک‌لیست‌ها در `src/data/*.ts` است. برای افزودن چک‌لیست جدید، از الگوی موجود در این فایل‌ها پیروی کنید.

## 📝 قوانین کدنویسی

- کد باید **TypeScript** و تایپ‌امن باشد.
- بدون کامنت‌های بلااستفاده و کد تکراری.
- فرمت کد با ترتیب پیش‌فرض و یکدست (تب / ۲ فاصله مطابق کدهای موجود) حفظ شود.
- رابط‌های کاربری باید RTL و فارسی باشند و به موبایل هم برسند.
- پیش از ارسال PR، حتماً `npm run lint` و `npm run build` موفق باشند.

## 🔃 ارسال Pull Request

1. از شاخه‌ی `main` کار را بروزرسانی کنید:

   ```bash
   git pull upstream main
   ```

2. تغییرات را push کرده و یک **Pull Request** کوتاه و خوانا باز کنید و شرح دهید:
   - چه تغییری کردید و چرا
   - اگر تغییر UI است، اسکرین‌شات ضمیمه کنید
   - در صورت رفع باگ، مراحل بازتولید را ذکر کنید

3. پس از بررسی، باگ‌ها و پیشنهادات Reviewer را اعمال کنید.

## 🐛 گزارش باگ و ایده

- برای ایده‌ها و باگ‌ها از تب **Issues** استفاده کنید.
- مشکلات امنیتی به‌صورت خصوصی (نه در Issues): [SECURITY.md](./SECURITY.md) را ببینید.

## 🧭 خط‌مشی کلی

پروژه یک مرجع آموزشی برای طراحان و توسعه‌دهندگان است؛ تغییرات باید **همگام با استانداردهای UI/UX/فرهنگ ایرانی** باشند و کیفیت محتوای چک‌لیست‌ها را ارتقا دهند.